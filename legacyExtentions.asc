// legacyExtentions script
// This script is used to keep the starterpack compatible with code using older versions of MMM specific functionality.
// If you don't use any old code, you can remove this script from the project without breaking something.

// **********************************************
// legacy code from AGS 3.3 and before
function EnterRoom(this Character*, int room, int x, int y, eDirection dir)
{
  this.FaceDirection(dir);
  this.ChangeRoom(room, x, y);
	return 1;
}

// **********************************************
// legacy code from ancient AGS 2.62 times.
bool FaceDirection(int charac,  eDirection dir)
{
	return character[charac].FaceDirection(dir);
}
function EnterRoom(int room, int x, int y, int dir)
{
	return player.EnterRoom(room, x, y, dir);
}

